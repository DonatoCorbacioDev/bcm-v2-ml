# --- Connessione MySQL ---
# Locale (MySQL esposto sulla 3307 dal docker-compose):
DB_URL=mysql+pymysql://bcm_user:bcm_password@localhost:3307/bcm

# In Docker Compose (stesso network, nome service mysql):
# DB_URL=mysql+pymysql://bcm_user:bcm_password@mysql:3306/bcm

# --- CORS ---
# Origini browser ammesse (separare con virgola per aggiungerne altre)
CORS_ORIGINS=http://localhost:3000
